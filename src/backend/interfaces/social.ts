export interface SocialRow {
  id: string;
  artistId: string;
  name: string;
  description: string;
  url: string;
}

export interface SocialEntity {
  id: string;
  artistId: string;
  name: string;
  description: string;
  url: string;
}

export interface SocialApiDto {
  id: string;
  artistId: string;
  name: string;
  description: string;
  url: string;
}
