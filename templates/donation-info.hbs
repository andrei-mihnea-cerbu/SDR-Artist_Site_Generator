<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Donation Notification</title>
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet" />
  <style>
    body {
      font-family: 'Roboto', sans-serif;
      background-color: #f9f9f9;
      margin: 0;
      padding: 0;
    }
    .container {
      max-width: 500px;
      margin: 40px auto;
      background-color: #ffffff;
      border-radius: 10px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
      padding: 30px;
    }
    h1 {
      margin-bottom: 20px;
      font-size: 22px;
      color: #333;
      text-align: center;
    }
    p {
      font-size: 16px;
      color: #444;
      margin: 12px 0;
    }
    .highlight {
      font-weight: bold;
      color: #222;
    }
    .donor-email {
      color: #0070BA;
      text-decoration: none;
      font-weight: 500;
    }
    .donor-email:hover {
      text-decoration: underline;
    }
    .message-box {
      background-color: #f3f3f3;
      border-left: 4px solid #0070BA;
      padding: 12px 16px;
      border-radius: 6px;
      margin-top: 10px;
      font-style: italic;
      color: #333;
    }
    .message-box p {
      margin: 6px 0;
    }
    hr {
      border: 0;
      height: 1px;
      background: linear-gradient(to right, rgba(0, 0, 0, 0), #ccc, rgba(0, 0, 0, 0));
      margin: 25px 0;
    }
    .footer {
      font-size: 13px;
      color: #777;
      font-style: italic;
      text-align: center;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>🎉 New Donation Received</h1>
    <p>Dear {{artistName}},</p>
    <p>
      {{donorName}}
      (<a href="mailto:{{donorEmail}}" class="donor-email">{{donorEmail}}</a>)
      just donated
      <span class="highlight">{{amount}} {{currency}}</span>.
    </p>

    {{#if message}}
    <p><strong>Message from the donor:</strong></p>
    <div class="message-box">
      {{#each message}}
        <p>{{this}}</p>
      {{/each}}
    </div>
    {{/if}}

    <hr />

    <p class="footer">
      This is an automatic notification from the SDR system.
    </p>
  </div>
</body>
</html>
